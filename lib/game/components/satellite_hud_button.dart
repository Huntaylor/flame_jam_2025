import 'package:flame/input.dart';

class SatelliteHudButton extends HudButtonComponent {
  SatelliteHudButton(
      {super.onPressed,
      super.size,
      super.anchor,
      super.scale,
      super.position,
      super.priority,
      super.button,
      super.buttonDown});
}
